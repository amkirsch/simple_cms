<%= link_to("<< Back to List", subjects_path, class: 'back-to-link') %>

<div class="subjects edit">
  <h2>Update Subject</h2>

  <%= form_for(@subject) do |f| %>

    <%= render(:partial => "form", locals: {f: f}) %>

    <div class="form-buttons">
      <%= submit_tag("Edit Subject") %>
    </div>

  <% end %>

</div>

  <!-- Same as above, essentially  <%= form_for :subject, url: subject_path(@subject.id), html: { method: :patch } do |f| %>

    <%= render(:partial => "form", locals: {f: f}) %>

    <% end %>
