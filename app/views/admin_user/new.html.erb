<%= link_to("<< Back to List", {action: 'index'}, class: 'back-to-link') %>

<div class="admin_users new">
  <h2>Create User</h2>

  <%= form_for(:admin_user, url: {action: 'create'}) do |f| %>

    <%= render(:partial => "form", locals: {f: f}) %>

    <div class="form-buttons">
      <%= submit_tag("Create User") %>
    </div>

  <% end %>
</div>
